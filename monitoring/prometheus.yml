global:
  scrape_interval: 2s # Padrão é a cada 1 min
  evaluation_interval: 2s # Padrão é a cada 1 min

scrape_configs:
  - job_name: 'gateway'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']

  - job_name: 'user-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8081']

  - job_name: 'eureka-server'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8761']

  - job_name: 'ai-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8090']

  - job_name: 'config-server'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8888']

  # - job_name: 'prometheus'
  #   static_configs:
  #     - targets: ["host.docker.internal:9090"]
  #       labels:
  #         app: 'prometheus'

### Comando prometheus
# docker run -d `
#   --name prometheus `
#   -p 9090:9090 `
#   -v "${PWD}\prometheus.yml:/etc/prometheus/prometheus.yml" `
#   prom/prometheus

### comando grafana
# docker run -d `
#   --name grafana `
#   -p 3000:3000 `
#   grafana/grafana

### dados grafana
# usuário: admin
# senha: admin